.list-group
    if categories.length > 0 
        each category in categories
            a(href=`/categories/${category.id}` class=category.id==selectedCategory?"list-group-item active":"list-group-item").list-group-item-action.list-group-item-secondary #{category.name}
            
            